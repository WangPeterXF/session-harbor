interface:
  display_name: "SessionHarbor"
  short_description: "Codex backup, cleanup, restore"
  default_prompt: "Use $session-harbor as my session management center and show the read-only dashboard before proposing changes."
